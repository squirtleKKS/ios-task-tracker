# Лабораторная №8

Что было сделано

Реализован универсальный экран для Backend Driven UI, который загружает JSON-конфигурацию с сервера, декодирует её в модель и отображает через маппер из ЛР7.

Контроллер стал полностью абстрактным: он не содержит UI-логики и не зависит от конкретного экрана. Вся верстка и структура интерфейса формируются на основе данных с бэка.

Допы:
- не делал